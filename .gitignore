# Build output
[Bb]in/
[Oo]bj/
.vs/
*.user
*.suo

# Test / coverage artifacts
TestResults/
[Cc]overage/
*.coverage
*.cobertura.xml
*.opencover.xml
lcov.info

# Deployment config (clientId, authority) and user settings (account email / PII).
# Templates appsettings.example.json / settings.example.json are versioned instead.
appsettings.json
settings.json

# MSAL token cache (normally under %LOCALAPPDATA%, never committed)
msal.cache
*.msalcache.bin

# Assistant working notes and local agent guidance
.remember/
CLAUDE.md

# Internal design docs (specs, plans, UI design) — not part of the repo
docs/

# OS noise
Thumbs.db
.DS_Store
