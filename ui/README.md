# Zync Master UI

Open `index.html` directly in any modern browser. No build step, no dependencies, no network — it runs on mock data standalone, and routes through the native bridge when embedded in the desktop shell.
